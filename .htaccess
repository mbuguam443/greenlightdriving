# Green Light Driving School - cPanel Passenger .htaccess
# Passenger handles all requests. Apache serves /static/ only.

RewriteEngine On

# /static/ — serve directly via Apache if file exists
RewriteCond %{REQUEST_URI} ^/static/
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]
